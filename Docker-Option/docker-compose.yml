version: '3.8'

services:
  # The Web Server (Apache + PHP)
  web:
    build: .
    container_name: kidgames-app
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    depends_on:
      - db
    networks:
      - kidgames-network

  # The Database (MariaDB)
  db:
    image: mariadb:latest
    container_name: kidgames-db
    environment:
      # Updated to MARIADB_ prefix for modern compatibility
      MARIADB_ROOT_PASSWORD: rootpassword
      MARIADB_DATABASE: kidgames
      MARIADB_USER: kid_user
      MARIADB_PASSWORD: kid_password
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - kidgames-network

volumes:
  db_data:

networks:
  kidgames-network:
    driver: bridge
